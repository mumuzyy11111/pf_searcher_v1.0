# 更新文档

## 2026-07-01 状态记录器来源明细新增数值类型

本次优化人物状态记录器右侧详情栏的来源明细表格。

### 已完成内容

- 更新 `web/assets/js/status-tracker.js`，来源行新增 `type` 字段，形成“数值 / 数值类型 / 来源 / 删除”四列结构。
- 更新旧来源行兼容逻辑，没有 `type` 的历史来源会自动补为空字符串。
- 更新 `web/assets/css/status-tracker.css`，将详情抽屉加宽到 `min(520px, 96vw)`，并调整来源明细列宽。
- 更新 `tests/test_status_tracker_static.py`，覆盖数值类型字段、输入绑定和更宽的详情栏样式。
## 2026-07-01 修复状态记录器添加来源按钮

本次修复人物状态记录器右侧详情栏中“添加来源”按钮无响应的问题。

### 已完成内容

- 更新 `web/assets/js/status-tracker.js`，在点击事件处理器中接入 `add-detail-modifier` 和 `remove-detail-modifier` 动作。
- 更新 `tests/test_status_tracker_static.py`，新增回归测试，确认来源行添加/删除动作已经绑定到点击处理器。
## 2026-06-30 人物状态记录器详情来源行

本次将“基础数值”详情栏从普通字段表单改为“基础值 + 来源明细”的结构。

### 已完成内容

- 更新 `web/assets/js/status-tracker.js`，新增 `basicStats.detailModifiers` 数据结构，用于按数值项记录来源行。
- 右侧详情栏不再显示备注字段；除基础值外，每条修正都以“数值 / 来源”单独成行。
- HP 详情保留最大 HP、当前 HP、临时 HP、非致命伤害四项，其中最大 HP 支持基础值和来源明细计算。
- 旧档案会在加载时迁移：旧的 AC、属性增强/临时/其他、豁免修正会转成来源行。
- 更新 `web/assets/css/status-tracker.css`，新增来源明细表格、来源输入、数值输入和删除按钮样式。
- 更新 `tests/test_status_tracker_static.py`，覆盖来源行、增删来源、HP 最大值基础字段和详情栏无备注字段。
## 2026-06-30 人物状态记录器详情栏可收缩

本次将“基础数值”的详情栏从固定右侧区域改为默认收起的右侧抽屉。

### 已完成内容

- 更新 `web/assets/js/status-tracker.js`，使详情栏默认不显示；点击数值卡片后打开详情抽屉；点击关闭按钮或遮罩后收起。
- 更新 `web/assets/css/status-tracker.css`，新增右侧抽屉、遮罩、关闭按钮和移动端底部抽屉样式。
- 更新 `web/status_tracker.html` 与 `web/index.html` 的资源版本号，确保浏览器加载新版抽屉交互。
- 更新 `tests/test_status_tracker_static.py`，覆盖默认收起、打开详情和关闭详情的关键标记。
## 2026-06-30 人物状态记录器基础数值总览

本次优化人物卡状态记录器的“基础数值”栏位，将原本一次性展开的完整表单改为“主界面最终值总览 + 右侧详情构成”的显示方式。

### 已完成内容

- 更新 `web/assets/js/status-tracker.js`，新增基础数值总览卡片、详情栏状态、最终值计算函数和详情字段配置。
- 更新 `web/assets/css/status-tracker.css`，新增总览网格、数值卡片、右侧详情栏和移动端适配样式。
- 更新 `web/status_tracker.html` 与 `web/index.html` 的资源版本号，避免浏览器缓存旧版状态器资源。
- 更新 `tests/test_status_tracker_static.py`，覆盖总览渲染入口、详情栏入口、关键详情 ID 和样式标记。

### 当前范围

第一版只覆盖基础数值：属性、HP、AC、豁免、BAB、近战/远程攻击、CMB/CMD、先攻和速度。法术、职业能力、专长、Buff、奇物和剧情备注仍保持列表填写方式。

## 2026-06-29 项目改名为 pathfinder_tools_v1.2.2

本次将整个大项目的对外名称统一为 `pathfinder_tools_v1.2.2`。

### 已完成内容

- 在 `pf_rag/version.py` 中新增项目名常量 `APP_NAME = "pathfinder_tools"`，并将版本号更新为 `1.2.2`。
- 更新 `run_lite.py`、`run_web.py`、`api/main.py`、`pf_rag/runtime/lite_app.py` 的启动输出和 API 标题。
- 更新 `web/index.html`、`web/character.html`、`web/status_tracker.html` 的页面标题和主标题。
- 更新 `scripts/package/package_lite.py` 与 `scripts/package/package_portable.py` 的打包产物名、安装目录、快捷方式名称和随包说明。
- 更新 README、代码结构说明、代码总览、角色卡 schema 文档和配置脚本注释。

### 命名规则

- 项目名：`pathfinder_tools`
- 当前版本：`1.2.2`
- 对外发布名：`pathfinder_tools_v1.2.2`
- 完整版便携包：`pathfinder_tools_v1.2.2_portable.zip`
- 精简版便携包：`pathfinder_tools_lite_v1.2.2_portable.zip`

## 2026-06-29 人物卡状态记录器第一步

本次新增人物卡状态记录器，作为与“车卡器”“资料查询”并列的第三个主模块。

### 已完成内容

- 新增 `web/status_tracker.html`，提供独立的人物卡状态记录页面。
- 新增 `web/assets/js/status-tracker.js`，支持多角色状态档案、自动保存、JSON 导入导出。
- 新增 `web/assets/css/status-tracker.css`，提供状态记录器页面样式。
- 更新 `web/index.html` 和 `web/assets/js/index.js`，在首页加入“人物卡状态记录器”分组与 `statusTracker` 面板。
- 新增 `tests/test_status_tracker_static.py`，覆盖主入口注册、页面资源、8 个可填写栏位和核心字段。

### 第一阶段范围

本阶段完成“完整可填写”：

- 基础数值：HP、AC、豁免、速度、BAB、攻击加值、CMB/CMD、先攻、种族、属性等。
- 职业能力：职业、等级、每级能力、使用次数、效果和备注。
- 法术：施法职业、法术位、法术 DC、法术列表和备注。
- 当前专长：专长名、类型、来源、效果、常驻状态和备注。
- 当前 Buff：名称、来源、持续时间、剩余时间、启用状态、影响字段和效果。
- 其他生物：召唤物、动物伙伴、魔宠、盟友等的关键状态与效果。
- 奇物：物品名、部位、装备/启用状态、使用次数和效果。
- 剧情备注：当前任务、NPC、地点、线索、战利品、待办和自由备注。

自动计算与联动属于第二阶段，第一阶段只保留结构化字段，为后续计算做准备。

## 2026-06-26 项目内对齐 GitHub main 更新

本次对齐基于 GitHub `main` 最新提交 `a6ce90d`，本地仓库已快进同步到 `origin/main`。

### 已对齐内容

- 恢复 `requirements.txt`，避免 README 中的安装命令指向不存在的依赖文件。
- 更新 `README.md`，补充 GitHub 更新引入的状态查询、组合式法术筛选和内容完整性修复工具。
- 更新 `docs/CODEBASE_ORGANIZATION.md`，补充新增前端页面、状态数据文件、组合筛选脚本和修复工具说明。
- 更新 `Apply-Repair.ps1`，在入口处设置 UTF-8 输入/输出，并保留默认 dry-run、`-Apply` 写入的使用方式。

### GitHub 更新带来的新增能力

- `web/conditions.html`：状态查询页面。
- `web/assets/data/conditions.json`：状态规则静态数据。
- `web/assets/js/conditions.js` 与 `web/assets/css/conditions.css`：状态查询交互与样式。
- `web/spells_filter.html`：组合式法术筛选页面。
- `web/assets/js/spell-filter-core.js`、`web/assets/js/spell-filter.js`：组合筛选逻辑。
- `tools/repair_content_integrity.py`：内容完整性修复工具，用于法术职业别名归一、法术职业/环位去重和专长文本污染修复。

### 使用说明

默认只生成审计报告：

```powershell
.\Apply-Repair.ps1
```

确认审计结果后再写入修复：

```powershell
.\Apply-Repair.ps1 -Apply
```

### 验证范围

- Python 修复工具语法检查。
- 新增/更新前端 JavaScript 语法检查。
- 轻量版 FastAPI 应用创建与静态页面路径检查。
